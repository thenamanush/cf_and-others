#include<bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    ll n, x; cin >> n >> x;
    vector<ll> q(n), r(n);

    for(int i = 0; i < n; ++i) cin >> q[i];
    for(int i = 0; i < n; ++i) cin >> r[i];
}

int main() {
    int t; cin >> t;
    while(t--) solve();
}