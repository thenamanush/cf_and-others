#include <bits/stdc++.h>
using namespace std;
#define alliswell                     \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);

#define int long long
#define endl '\n'

void solve()
{
    string s;
    int n;
    cin >> s >> n;
    vector<int> a(n);

    /*
    observations:
    b is a little bigger than a
    b is little smaller than a
    b is equal size of a
    */

    for (int i = 0; i < n; ++i)
        cin >> a[i];
}

int32_t main()
{
    alliswell

        int t = 1;
    cin >> t;
    while (t--)
        solve();

    return 0;
}