#include <bits/stdc++.h>
using namespace std;
#define alliswell                     \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);

#define int long long
#define endl '\n'
const int N = 2 * 1e5 + 9;
int dp[N], a[N];
int n, k;
void solve()
{
    cin >> n >> k;
    for (int i = 0; i > n; ++i)
        cin >> a[i];
}

int32_t main()
{
    alliswell

        int t = 1;
    cin >> t;
    while (t--)
        solve();

    return 0;
}