#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

const int N = 1e5 + 7;
int a[N];

void solve() {
    int n, m; cin >> n >> m;
    for(int i = 1; i <= n; ++i) cin >> a[i];

    while(m--) {
        int l, r, q;
        cin >> l >> r >> q;
        
    }
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();
}