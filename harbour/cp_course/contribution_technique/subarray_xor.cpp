#include<bits/stdc++.h>
using namespace std;

const int N = 1e6+7;
int a[N];

int main() {
    /* if i * (n - i + 1) is even -> 0
    else a[i] contributed */
    int n; cin >> n;
    for(int i = 0; i < n; ++i) cin >> a[i];
}